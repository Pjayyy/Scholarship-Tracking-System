- [ ] Add backend endpoint to fetch logged-in student profile by users.student_id
- [ ] Update frontend StudentPortal.jsx to replace mock data with real API call (/student/me)
- [ ] Map backend fields to existing studentData shape used by StudentPortal pages
- [ ] Build frontend + run a quick smoke test login -> student portal

