Next fix required: backend/server.js currently hardcodes MySQL database "scholarship_system".
Error log shows: Unknown database 'scholarship_system'.
So /attendance cannot see students.

Fix approach:
- Replace hardcoded database with process.env.DB_NAME (same pattern as backend/db.js).
- Ensure .env contains DB_HOST, DB_USER, DB_PASSWORD, DB_NAME.

After DB alignment:
- Re-test scanning; "Student not found" should only occur for real missing students.

