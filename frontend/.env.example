# CRA environment variables for configuring the backend API URL
# Create a .env file (NOT .env.example) in this folder and copy the lines you want.

# Single backend URL
# REACT_APP_API_URL=http://localhost:5000

# Or multiple candidates (comma-separated). The app will try them in order.
REACT_APP_API_URLS=http://localhost:5000

