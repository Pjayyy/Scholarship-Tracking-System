// Fallback override entry for react-app-rewired.
// This project currently uses react-scripts.
// Prefer craco.config.js if installing craco.
module.exports = function override(config, env) {
  return config;
};

